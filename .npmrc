# Block all lifecycle scripts by default
# This prevents postinstall/preinstall/install scripts from running automatically
# When Claude or you run npm install, no package can execute code on your system
# To run scripts for legitimate packages (like sharp), use: npm rebuild sharp
ignore-scripts=true
